import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:miracle_morning_app/app/modules/chosen_done/chosen_done_page.dart';

void main() {
  testWidgets('ChosenDonePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChosenDonePage(title: 'ChosenDone')));
    //  final titleFinder = find.text('ChosenDone');
    //  expect(titleFinder, findsOneWidget);
  });
}
