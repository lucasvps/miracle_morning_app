import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:miracle_morning_app/app/modules/chosen_done/chosen_done_controller.dart';
import 'package:miracle_morning_app/app/modules/chosen_done/chosen_done_module.dart';

void main() {
  initModule(ChosenDoneModule());
  // ChosenDoneController chosendone;
  //
  setUp(() {
    //     chosendone = ChosenDoneModule.to.get<ChosenDoneController>();
  });

  group('ChosenDoneController Test', () {
    //   test("First Test", () {
    //     expect(chosendone, isInstanceOf<ChosenDoneController>());
    //   });

    //   test("Set Value", () {
    //     expect(chosendone.value, equals(0));
    //     chosendone.increment();
    //     expect(chosendone.value, equals(1));
    //   });
  });
}
