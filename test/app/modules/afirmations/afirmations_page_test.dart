import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('AfirmationsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AfirmationsPage(title: 'Afirmations')));
    //  final titleFinder = find.text('Afirmations');
    //  expect(titleFinder, findsOneWidget);
  });
}
