import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('CategoryChosenPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CategoryChosenPage(title: 'CategoryChosen')));
    //  final titleFinder = find.text('CategoryChosen');
    //  expect(titleFinder, findsOneWidget);
  });
}
