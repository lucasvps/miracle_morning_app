import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:miracle_morning_app/app/modules/quote/quote_controller.dart';
import 'package:miracle_morning_app/app/modules/quote/quote_module.dart';

void main() {
  initModule(QuoteModule());
  // QuoteController quote;
  //
  setUp(() {
    //     quote = QuoteModule.to.get<QuoteController>();
  });

  group('QuoteController Test', () {
    //   test("First Test", () {
    //     expect(quote, isInstanceOf<QuoteController>());
    //   });

    //   test("Set Value", () {
    //     expect(quote.value, equals(0));
    //     quote.increment();
    //     expect(quote.value, equals(1));
    //   });
  });
}
