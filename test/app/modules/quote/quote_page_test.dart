import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('QuotePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(QuotePage(title: 'Quote')));
    //  final titleFinder = find.text('Quote');
    //  expect(titleFinder, findsOneWidget);
  });
}
