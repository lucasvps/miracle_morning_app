import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('CategoryDetailPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CategoryDetailPage(title: 'CategoryDetail')));
    //  final titleFinder = find.text('CategoryDetail');
    //  expect(titleFinder, findsOneWidget);
  });
}
